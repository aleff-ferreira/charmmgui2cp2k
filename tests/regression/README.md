# Regression tests (golden outputs)

Checksummed reference CP2K inputs and audit artifacts for small fixture systems.
Any change that perturbs generated output must diff against these baselines.
Populated in Phase 2. Mark tests with `@pytest.mark.regression`.
