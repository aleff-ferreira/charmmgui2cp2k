# Unit tests (scientific core)

Fast, data-light unit tests for the scientific functions in `charmmgui2cp2k.py`
(element inference, charge redistribution, ring / pi-stacking geometry, version
parsing, electron counting, link-bond detection). Populated in Phase 2 of the
publication-readiness plan. Mark tests with `@pytest.mark.unit`.
